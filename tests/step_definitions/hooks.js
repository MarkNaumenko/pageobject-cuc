'use strict';

var isStart = true;

module.exports = function () {

    this.setDefaultTimeout(60000);

    this.After(function () {

    });

    this.After(function (scenario) {

    });

    this.After(function (scenario) {

    });

    this.Before(function () {

    });

};